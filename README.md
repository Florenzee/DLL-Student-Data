# DLL-Student-Data
Implementation of a double linked list (DLL) in C language to store student name and grade data, with several functions to add data, swap node positions, sort data by grade, and print a list of students.
